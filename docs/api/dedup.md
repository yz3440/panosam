# Deduplication

The deduplication module handles the removal of duplicate masks that appear when the same object is visible in multiple perspective views.

## SphereMaskDeduplicationEngine

::: panosam.dedup.detection.SphereMaskDeduplicationEngine
    options:
      show_root_heading: true
      members:
        - __init__
        - check_duplication
        - deduplicate_list
        - deduplicate_frames

## Data Classes

::: panosam.dedup.detection.PolygonIntersection
    options:
      show_root_heading: true
